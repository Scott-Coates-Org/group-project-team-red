import styled from 'styled-components'

export const StyledInfoBlue = styled.div`
  width: ${({ width }) => (width ? width : '100%')};
  background: lightblue;
  color: #333;
  padding: 0.3em 0.5em;
  margin: 0.4em 0;
`
